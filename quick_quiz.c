#include <stdio.h>
#include<math.h>

int main(){
        int n;
        printf("Enter the side of the square :");
        scanf("%d", &n);
        printf("The area of this square is %f\n", pow(n, 2));
return 0;
}