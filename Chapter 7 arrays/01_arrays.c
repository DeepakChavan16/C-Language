//An array is a collection of similar elements. Array allows a single
//variable to store multiple values.

#include <stdio.h>

int main(){
    int marks[90];
    
    marks[0] = 45;
    marks[1] = 94;
    printf("Marks 0 and Marks 1 is %d %d", marks[0], marks[1]);
return 0;
}